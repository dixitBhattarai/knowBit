#ifndef DASHBOARD_H
#define DASHBOARD_H

// Returns true if the user clicked Logout (caller should show the login
// screen again). Returns false if the window itself was closed (caller
// should quit the whole program).
bool Dashboard();

#endif 
